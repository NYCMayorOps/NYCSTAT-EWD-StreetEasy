name: Daily Data Refresh

on:
  schedule:
    - cron: '0 7 * * *'  # 7:00 AM UTC (2am New York EST / 3am EDT)
  workflow_dispatch:      # Manual trigger

jobs:
  refresh:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run StreetEasy transformation
        run: python transform.py

      - name: Run Times Square transformation
        run: python transform_timessquare.py

      - name: Commit and push if changed
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add data/
          git diff --staged --quiet || git commit -m "Update data $(date -u +%Y-%m-%d)"
          git push
